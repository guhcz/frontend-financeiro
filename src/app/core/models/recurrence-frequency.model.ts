export type RecurrenceFrequency = 'MONTHLY';

export const RECURRENCE_FREQUENCIES: RecurrenceFrequency[] = ['MONTHLY'];

export const RECURRENCE_FREQUENCY_LABELS: Record<RecurrenceFrequency, string> = {
  MONTHLY: 'Mensal',
};
